# railway.toml — deploy from services/extractor as the Railway root
# Dashboard: Settings → Root Directory = services/extractor

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
